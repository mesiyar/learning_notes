# linux 常用命令
kill
```shell 
#kill -9 和 kill -15 的区别
kill -15 或者是 kill 
当执行上述命令时,会发生一下事情
1.程序like停止
2.当程序释放资源后停止
3.程序可能还会继续运行
大部分程序接收到SIGTERM信号后，会先释放自己的资源，然后在停止。但是也有程序可以在接受到信号量后，做一些其他的事情，并且这些事情是可以
配置的。如果程序正在等待IO，可能就不会立马做出相应。也就是说，SIGTERM多半是会被阻塞的、忽略。

kill -9 
当执行上述命令时,程序无须想用SIGTERM 直接杀死该程序
```


